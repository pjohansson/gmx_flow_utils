# Runtime dependencies
matplotlib >= 3.0
numpy >= 1.20 # Minimum tested version, likely compatible with some older ones
scipy >= 1.6

# Build / install dependencies
pytest >= 6.2
setuptools >= 42
wheel